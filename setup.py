from setuptools import find_packages, setup

setup(
    name='infini_websearch',
    version='0.1.0',
    description=(
        "A demo built on Megrez-3B-Instruct,"
        "integrating a web search tool to enhance the model's question-and-answer capabilities."
    ),
    packages=find_packages(),
)
